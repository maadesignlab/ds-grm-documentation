import { Meta, Title, Source } from "@storybook/addon-docs/blocks"
import * as ProgressStories from "./progress.stories"
import primitiveSource from "./progress.tsx?raw"
import exampleSource from "./progress-example.tsx?raw"
import { ProgressComposition, ProgressSpecifications, ProgressValues } from "./progress-docs"

<Meta of={ProgressStories} />
<Title of={ProgressStories} />

## Versión

<details className="rounded-lg border border-border bg-card px-4 py-3" style={{ marginBottom: 48 }}>
  <summary><strong>v1.0.0</strong> · Implementación inicial desde Figma y shadcn/ui</summary>

  - Conserva el wrapper oficial `Progress` de shadcn/ui sobre Radix y reenvía `value` al `Root` para mantener `aria-valuenow`.
  - Mantiene `value`, semántica `progressbar`, estados ARIA y personalización mediante `className`.
  - El label y el porcentaje son composición externa y proporcionan el nombre accesible mediante `aria-labelledby`.
  - Incluye valores de 0 a 100 y responde a los tokens y tipografía de la marca activa.
  - Playground y Docs consumen `progressExamplePresets` y la misma instancia `ProgressExample`.
</details>

## Valores

<div style={{ marginBottom: 48 }}><ProgressValues /></div>

## Composición

<div style={{ marginBottom: 48 }}><ProgressComposition /></div>

## Especificaciones

<div style={{ marginBottom: 48 }}><ProgressSpecifications /></div>

## Código

### Primitive

<Source language="tsx" code={primitiveSource} />

### Composición de referencia

<Source language="tsx" code={exampleSource} />
