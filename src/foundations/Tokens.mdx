import { Meta } from '@storybook/addon-docs/blocks';
import { BrandTokensTable } from './BrandTokensTable';

<Meta title="Foundations/Tokens" />

# Tokens

Figma es la fuente de verdad. Los nombres de sus variables se conservan conceptualmente y se transforman a nombres CSS compatibles en `src/styles/tokens.css`.

La vista muestra una sola lista global de tokens. Sus valores y muestras de color se actualizan con la marca elegida en el selector de Storybook.

<BrandTokensTable />
